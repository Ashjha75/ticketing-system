export const environment = {
  production: true,
  // Base URL defaults to localhost:8080
  // Note: For runtime OS environment variables in a static Angular app,
  // you typically need a custom builder or a runtime config.json fetch.
  // For simplicity, we hardcode the fallback/default here.
  apiUrl: "http://localhost:8080",
};
